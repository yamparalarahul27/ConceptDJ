// Mock Supabase client to remove dependency for purely visual concepts
export const supabase = null as any;

// Helper to check if Supabase is configured
export const isSupabaseConfigured = () => false;
